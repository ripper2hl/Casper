{{!< default}}

{{{navigation}}}

<header class="main-header {{#if tag.image}}" style="background-image: url({{tag.image}}){{else}}{{#if @blog.cover}}" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}{{/if}}">
    <div class="container">
        <div class="main-header-text text-center">
            <h1 class="page-title courgette">{{tag.name}}</h1>
            <h2 class="page-description roboto">
              {{tag.description}}
            </h2>
        </div>
    </div>
</header>

{{! The main content area on the homepage }}
<main class="container" role="main">

    {{! The tag below includes the post loop - partials/loop.hbs }}
    {{> "loop"}}

</main>
